[phases.setup]
nixPkgs = ["nodejs", "openssh"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["cd backend && npx prisma generate"]

[start]
cmd = "cd backend && npx prisma migrate deploy && npx tsx src/index.ts"
