{
  "name": "matrix-hub",
  "private": true,
  "description": "Matrix Hub v2 — Sistema nervioso central del ecosistema",
  "scripts": {
    "start": "cd backend && npx prisma generate && npx prisma migrate deploy && npx tsx src/index.ts",
    "build": "cd frontend && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
