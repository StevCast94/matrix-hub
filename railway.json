{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "rootDirectory": "backend",
    "startCommand": "npx prisma generate && npx prisma migrate deploy && npx tsx src/index.ts",
    "restartPolicyType": "ON_FAILURE"
  }
}
