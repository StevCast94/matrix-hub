{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma generate && npx prisma db push && npx tsx src/index.ts",
    "restartPolicyType": "ON_FAILURE"
  }
}
