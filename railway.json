{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma generate && { npx prisma migrate resolve --applied 0_init 2>/dev/null || true; } && { npx prisma migrate deploy 2>/dev/null || npx prisma db push; } && npx tsx src/index.ts",
    "restartPolicyType": "ON_FAILURE"
  }
}
